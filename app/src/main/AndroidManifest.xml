<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.ticketingsystem">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Statics"></activity>
        <activity android:name=".AssignDriver" />
        <activity android:name=".AssignIspector" />
        <activity android:name=".AssignBus" />
        <activity android:name=".LauncherActivity" />
        <activity android:name=".RechargeActivity" />
        <activity android:name=".Homepage" />
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".AccountBalanceActivity" />
        <activity android:name=".RegisterActivity" />
    </application>

</manifest>